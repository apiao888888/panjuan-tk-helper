<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11+ 需要声明 queries 才能 getLaunchIntentForPackage 看到其他应用 -->
    <queries>
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.ss.android.ugc.trill" />
        <package android:name="com.tiktok.musically" />
        <package android:name="com.zhiliaoapp.musically.go" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TikTokAssistant"
        tools:targetApi="31">

        <!-- 授权激活界面（Launcher 入口） -->
        <activity
            android:name=".ui.LicenseActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 注册机界面（仅内部使用） -->
        <activity
            android:name=".ui.KeygenActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 主界面 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 任务配置界面 -->
        <activity
            android:name=".ui.TaskConfigActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 私信话术管理界面 -->
        <activity
            android:name=".ui.MessageTemplateActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- 调试探针界面（长按主界面版本号进入） -->
        <activity
            android:name=".ui.DebugProbeActivity"
            android:exported="false" />

        <!-- 悬浮窗服务 -->
        <service
            android:name=".service.FloatingWindowService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- 无障碍服务 -->
        <service
            android:name=".service.TikTokAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
